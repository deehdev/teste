package main

// caminhos dentro do container Docker
const (
    usersFile    = "/app/data/users.json"
    channelsFile = "/app/data/channels.json"
    subsFile     = "/app/data/subs.json"
    logsFile     = "/app/data/logs.json"
)
